# Word documents and project spec PDFs — local reference only, not part of the codebase
*.docx
*Spec*.pdf

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
env/
ds_env/

# Jupyter
.ipynb_checkpoints/

# Data science artifacts
*.pkl
*.joblib

# Trained model weights — large binary artifacts, over GitHub's 100MB limit.
# Reproduce by re-running the project notebook (see each project's README).
*.safetensors
*.bin
*.h5
*.pt
*.ckpt

# FAISS/vector index artifacts — regenerate by running the project notebook.
*.faiss
index/*.npy

# W&B local run logs
wandb/
